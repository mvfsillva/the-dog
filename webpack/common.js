const { join } = require('path')

const paths = {
  root: join(__dirname, '..'),
  src: join(__dirname, '..', 'src'),
  public: join(__dirname, '..', 'public'),
  build: join(__dirname, '..', 'build')
}
